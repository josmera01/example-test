{#
/**
 * @file crud_entity.html.twig
 * Default theme implementation to present Crud entity data.
 *
 * This template is used when viewing Crud entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_crud_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('crud_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
